<Window x:Class="OnePieceLauncherWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="One Piece Launcher" Height="450" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border Background="#1A1A1D" CornerRadius="15" BorderBrush="#C3073F" BorderThickness="2" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="120" />
            </Grid.RowDefinitions>

            <!-- Background Image Placeholder (Can be changed in code or designer) -->
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" Grid.RowSpan="3" Opacity="0.4" />

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="#99000000">
                <TextBlock Text="ONE PIECE - LAUNCHER" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Margin="15,0,0,0" />
                <Button Content="✕" Width="40" Height="40" HorizontalAlignment="Right" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" Click="BtnClose_Click" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF4A4A" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Main Content Area -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="PREPARE-SE PARA ZARPAR!" Foreground="#C3073F" FontSize="36" FontWeight="Black" FontFamily="Arial" TextAlignment="Center" />
                <TextBlock x:Name="lblVersion" Text="v0.0.0" Foreground="Gray" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0" />
            </StackPanel>

            <!-- Bottom Panel -->
            <Grid Grid.Row="2" Background="#DD000000">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="20,15,20,10" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Nome:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                        <TextBox x:Name="txtUsername" Width="150" Height="25" Background="#333" Foreground="White" BorderBrush="#555" Padding="5,2" VerticalContentAlignment="Center"/>
                        
                        <TextBlock Text="Gênero:" Foreground="White" VerticalAlignment="Center" Margin="20,0,10,0" FontSize="14"/>
                        <RadioButton x:Name="rbMale" Content="M" Foreground="White" IsChecked="True" VerticalAlignment="Center" Margin="0,0,10,0" Cursor="Hand" ToolTip="Masculino"/>
                        <RadioButton x:Name="rbFemale" Content="F" Foreground="White" VerticalAlignment="Center" Cursor="Hand" ToolTip="Feminino"/>
                    </StackPanel>

                    <TextBlock x:Name="lblStatus" Text="Verificando atualizações..." Foreground="LightGray" FontSize="12" Margin="0,0,0,5" />
                    <ProgressBar x:Name="progressBar" Height="8" Background="#333" Foreground="#C3073F" BorderThickness="0" />
                </StackPanel>

                <Button x:Name="btnPlay" Grid.Column="1" Content="JOGAR" Margin="10,20,20,20" Background="#C3073F" Foreground="White" FontWeight="Bold" FontSize="20" BorderThickness="0" IsEnabled="False" Click="BtnPlay_Click" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF1D58" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#555" />
                                    <Setter Property="Foreground" Value="#AAA" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
